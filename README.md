# Thermal Sentinel AI — Backend API

Backend service providing real-time satellite thermal anomaly aggregation, AI classification integration, GIS spatial matching, and incident management APIs.

## Quickstart Setup

1. **Clone & Setup Virtual Environment**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   pip install -r requirements.txt